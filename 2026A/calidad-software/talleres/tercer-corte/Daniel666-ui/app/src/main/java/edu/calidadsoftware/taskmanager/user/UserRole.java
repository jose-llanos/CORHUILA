package edu.calidadsoftware.taskmanager.user;

/**
 * Roles de autorización del sistema.
 *
 * USER: usuario estándar
 * ADMIN: administrador con permisos ampliados (p. ej. gestión de usuarios)
 */
public enum UserRole {
    USER,
    ADMIN
}
